# Enter / Exit ViewTransition Demo (React 19.3 canary)

Minimal Vite + React 19.3 canary app showing the **EnterExitDemo** example
that animates an element on enter and exit using `ViewTransition`.

## Scripts

```bash
npm install
npm run dev
```

Then open the URL that Vite prints (usually http://localhost:5173).
